
for f in `egrep -v '^\s*#' `
for f in /dir/*/*txt
for f in `cat list.txt`
do
    echo "$(/bin/date +"%Y-%m-%d_%H%M"): File: $f"
    echo "File: $f"
    #cmd
    #cmd
    #cmd
    echo "$(/bin/date +"%Y-%m-%d_%H%M")"
    echo
done

